#include <iostream>
#include <sstream>
#include <queue>
#include <vector>
#include <string>
using namespace std;
int main(){
    string line;
    if(!getline(cin,line)) return 0;
    if(line.size()==0) if(!getline(cin,line)) return 0;
    istringstream iss(line);
    string tok;
    vector<int> cnt(256,0);
    queue<char> q;
    while(iss >> tok){
        char c = tok[0];
        ++cnt[(unsigned char)c];
        q.push(c);
        while(!q.empty() && cnt[(unsigned char)q.front()]>1) q.pop();
        if(!q.empty()) cout<<q.front()<<" "; else cout<<"-1 ";
    }
    return 0;
}
